#include <iostream>
#include <vector>
#include <list>
#include <stack>
#include <queue>
using namespace std;

// 동적 배열
// 연결 리스트
// 스택
// 큐

// 동적 배열, 연결 리스트는 면접 단골 문제.
// 시간 복잡도도 중요함!

// 동적 배열 일반 삽입은 시간 복잡도 상수O(1)라 빠르게 동작. 대신 중간 삽입/삭제 취약 O(N)
// 임의 접근도 연속적으로 배치되어있어 금방 찾음 O(1).

// 연결 리스트 중간 삽입/삭제도 빠르게 동작 O(1). 임의 접근은 느리게 동작 O(N)

// 스택 O(1)
// 큐 O(1) 

int main()
{


	
	return 0;
}